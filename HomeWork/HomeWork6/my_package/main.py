# Import the modules
import math_utils
import string_utils

# Call the functions
print(math_utils.get_factorial(5))
print(math_utils.nsd(15, 150))

print(string_utils.str_to_uppercase("some string with some number 532523"))
print(string_utils.del_empty_spaces("   Some string with start and the end empty spaces     "))
